@model List<MovieViewModel>

@if (Model.Count > 0)
{
    foreach (var movie in Model)
    {
        @Html.Partial("../Partials/_MovieDetails", movie)
    }
}
else
{
    <p>No Theaters matched your search criteria! :(</p>
}
