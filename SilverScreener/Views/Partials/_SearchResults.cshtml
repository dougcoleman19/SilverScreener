@model List<TheaterViewModel>

@if (Model.Count > 0)
{
    foreach (var theater in Model)
    {
        @Html.Partial("../Partials/_TheaterDetails", theater)
    }
}
else
{
    <p>No Theaters matched your search criteria! :(</p>
}